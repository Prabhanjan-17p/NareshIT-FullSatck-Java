#include<stdio.h>
int main(){
    //1-Write a C program to compute the sum of all numerical values present in a given string?
    // char str[20]="a0b1c2";
    // int i, sum=0;
    // for (int i = 0; str[i] != '\0'; i++)
    // {
    //    if (str[i] >= 49 && str[i] <= 59)
    //    {
    //         sum += str[i] - 48; // ASCII value of '0' is 48, so subtracting 48 gives the actual numeric value of the character.
    //    }
    // }
    // printf("Sum of numerical values: %d\n", sum);

    //2-Write a C program to concatenate two strings into a single string ?     // strcat(str1, str2);
    // char str1[20] = "Hello";
    // char str2[20] = " World";
    // int s1=0,s2=0,s3=0;
    // for (int i = 0; str1[i] != '\0'; i++)
    // {
    //     s1++;
    // }
    // for (int i = 0; str2[i] != '\0'; i++)
    // {
    //     s2++;
    // }
    // s3 = s1+s2;
    // char str3[s3];
    // int j=0;
    // for (int i = 0; str1[i] != '\0'; i++)
    // {
    //     str3[j] = str1[i];
    //     j++;
    // }
    // for (int i = 0; str2[i] != '\0'; i++)
    // {
    //     str3[j] = str2[i];
    //     j++;
    // }
    // str3[s3] = '\0';  
    // printf("Concatenated string: %s\n", str3);


    //3-Write a C program to multiply two positive integers provided as strings ?
    // char str1[10] = "10";
    // char str2[10] = "15";
    // // scanf("%s",&str1);
    // // scanf("%s",&str2);
    // int sum1 =0;
    // int sum2 =0;
    // for (int  i = 0; str1[i] != '\0'; i++)
    // {
    //     sum1 = sum1*10 + (str1[i] - 48);
    // }
    // for (int  i = 0; str2[i] != '\0'; i++)
    // {
    //     sum2 = sum2*10 + (str2[i] - 48); 
    // }
    // int product = sum1*sum2;
    // printf("Product of the two numbers: %d\n", product);
    




    return 0;
}